# Third-party Rust crates

Generated by `cargo about` from `Cargo.lock` — do not edit. Run `mise run notices`
after changing a dependency; CI regenerates this file and fails if it has drifted.

`THIRD-PARTY.md` covers the vendored JavaScript and CSS. This file is the same
obligation for the Rust half: the crates below are compiled into the binaries this
project distributes, and MIT and Apache-2.0 both ask for their notice to travel
with them.

{{#each licenses}}
## {{{name}}}

Used by {{#each used_by}}{{#if @index}}, {{/if}}[{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}}){{/each}}

```
{{{text}}}
```

{{/each}}
