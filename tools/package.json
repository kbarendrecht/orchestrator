{
  "name": "orchd-tools",
  "private": true,
  "type": "module",
  "description": "Terminal-side tooling for orchd. playwright-core drives an already-installed Chrome, and the WebKit build that matches this exact version — the app runs in WebKitGTK, so Chrome alone cannot see a rendering fault that only happens there. Pinned, not caret: the browser build on disk is tied to the version.",
  "dependencies": {
    "playwright-core": "1.49.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
