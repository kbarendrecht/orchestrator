{
  "//": "Type-checks the SPA without building it. The shipped files stay hand-written",
  "//2": "JavaScript — there is no emit step and no bundler; `noEmit` is the point.",
  "//3": "`snapshot.d.ts` is generated from the Rust structs by `cargo test`, so a",
  "//4": "renamed field on the daemon side fails here rather than rendering as blank.",
  "//5": "`strict`, reached in three passes and worth every one: `$` returning",
  "//6": "null cost 268 of the 400 `strictNullChecks` errors, three fields the",
  "//7": "review overlay reads were never sent by the daemon, and `diffState.anchors`",
  "//8": "was annotated `number[]` while holding elements. The 478 parameter",
  "//9": "annotations are typed against the ts-rs exports wherever a struct exists.",
  "compilerOptions": {
    "noEmit": true,
    "allowJs": true,
    "checkJs": true,
    "noFallthroughCasesInSwitch": true,
    "strict": true,
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": false
  },
  "include": ["../web/**/*.js", "../web/**/*.d.ts"],
  "exclude": ["../web/vendor/**"]
}
